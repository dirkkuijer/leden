
{% trans_default_domain 'FOSUserBundle' %}

{% block stylesheet %}
           {{ parent() }}
{% endblock %}

<h3>invullen</h3>

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="button primary" type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>
{{ form_end(form) }}
